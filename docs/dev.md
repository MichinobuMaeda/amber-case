# Development

## Preriquisites

- Node.js >= 14
- curl
- GitHub CLI (optional)

```
$ npm -g install yarn
$ git clone git@github.com:MichinobuMaeda/amber-case.git
$ cd amber-case
$ yarn --cwd functions install
$ yarn install
$ yarn test
$ yarn start
```
